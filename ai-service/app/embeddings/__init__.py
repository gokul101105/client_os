"""Generates vector embeddings from text for semantic search.

Will wrap whichever embedding model/provider is chosen, turning document
chunks (and user queries) into vectors that rag/ can compare. Added when
RAG is implemented.
"""
